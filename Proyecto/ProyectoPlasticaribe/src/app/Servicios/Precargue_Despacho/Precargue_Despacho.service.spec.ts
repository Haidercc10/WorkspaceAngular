/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Precargue_DespachoService } from './Precargue_Despacho.service';

describe('Service: Precargue_Despacho', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Precargue_DespachoService]
    });
  });

  it('should ...', inject([Precargue_DespachoService], (service: Precargue_DespachoService) => {
    expect(service).toBeTruthy();
  }));
});
