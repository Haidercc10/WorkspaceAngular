/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { TipoSalidas_CajaMenorService } from './TipoSalidas_CajaMenor.service';

describe('Service: TipoSalidas_CajaMenor', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TipoSalidas_CajaMenorService]
    });
  });

  it('should ...', inject([TipoSalidas_CajaMenorService], (service: TipoSalidas_CajaMenorService) => {
    expect(service).toBeTruthy();
  }));
});
