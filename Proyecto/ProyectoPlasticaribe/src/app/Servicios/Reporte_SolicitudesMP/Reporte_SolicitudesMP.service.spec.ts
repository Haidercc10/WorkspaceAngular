/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Reporte_SolicitudesMPService } from './Reporte_SolicitudesMP.service';

describe('Service: Reporte_SolicitudesMP', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Reporte_SolicitudesMPService]
    });
  });

  it('should ...', inject([Reporte_SolicitudesMPService], (service: Reporte_SolicitudesMPService) => {
    expect(service).toBeTruthy();
  }));
});
