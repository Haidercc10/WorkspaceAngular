/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Web_ContactoCorreoService } from './Web_ContactoCorreo.service';

describe('Service: Web_ContactoCorreo', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Web_ContactoCorreoService]
    });
  });

  it('should ...', inject([Web_ContactoCorreoService], (service: Web_ContactoCorreoService) => {
    expect(service).toBeTruthy();
  }));
});
