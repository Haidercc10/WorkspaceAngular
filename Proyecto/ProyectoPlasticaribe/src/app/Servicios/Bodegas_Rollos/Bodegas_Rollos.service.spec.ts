/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Bodegas_RollosService } from './Bodegas_Rollos.service';

describe('Service: Bodegas_Rollos', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Bodegas_RollosService]
    });
  });

  it('should ...', inject([Bodegas_RollosService], (service: Bodegas_RollosService) => {
    expect(service).toBeTruthy();
  }));
});
