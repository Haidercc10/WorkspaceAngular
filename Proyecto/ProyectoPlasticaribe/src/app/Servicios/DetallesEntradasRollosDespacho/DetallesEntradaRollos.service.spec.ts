/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DetallesEntradaRollosService } from './DetallesEntradaRollos.service';

describe('Service: DetallesEntradaRollos', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DetallesEntradaRollosService]
    });
  });

  it('should ...', inject([DetallesEntradaRollosService], (service: DetallesEntradaRollosService) => {
    expect(service).toBeTruthy();
  }));
});
