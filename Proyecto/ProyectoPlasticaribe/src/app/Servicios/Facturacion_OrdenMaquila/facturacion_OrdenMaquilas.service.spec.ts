/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Facturacion_OrdenMaquilasService } from './facturacion_OrdenMaquilas.service';

describe('Service: Facturacion_OrdenMaquilas', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Facturacion_OrdenMaquilasService]
    });
  });

  it('should ...', inject([Facturacion_OrdenMaquilasService], (service: Facturacion_OrdenMaquilasService) => {
    expect(service).toBeTruthy();
  }));
});
