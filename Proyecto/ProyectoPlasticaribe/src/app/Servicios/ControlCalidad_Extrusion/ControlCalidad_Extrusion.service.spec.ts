/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ControlCalidad_ExtrusionService } from './ControlCalidad_Extrusion.service';

describe('Service: ControlCalidad_Extrusion', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ControlCalidad_ExtrusionService]
    });
  });

  it('should ...', inject([ControlCalidad_ExtrusionService], (service: ControlCalidad_ExtrusionService) => {
    expect(service).toBeTruthy();
  }));
});
