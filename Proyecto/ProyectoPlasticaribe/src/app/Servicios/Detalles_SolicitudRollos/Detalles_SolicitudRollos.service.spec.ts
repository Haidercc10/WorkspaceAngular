/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Detalles_SolicitudRollosService } from './Detalles_SolicitudRollos.service';

describe('Service: Detalles_SolicitudRollos', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Detalles_SolicitudRollosService]
    });
  });

  it('should ...', inject([Detalles_SolicitudRollosService], (service: Detalles_SolicitudRollosService) => {
    expect(service).toBeTruthy();
  }));
});
