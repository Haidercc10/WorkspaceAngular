/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Vistas_PermisosService } from './Vistas_Permisos.service';

describe('Service: Vistas_Permisos', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Vistas_PermisosService]
    });
  });

  it('should ...', inject([Vistas_PermisosService], (service: Vistas_PermisosService) => {
    expect(service).toBeTruthy();
  }));
});
