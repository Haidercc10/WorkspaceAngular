/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { IngRollos_ExtrusuionService } from './IngRollos_Extrusuion.service';

describe('Service: IngRollos_Extrusuion', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [IngRollos_ExtrusuionService]
    });
  });

  it('should ...', inject([IngRollos_ExtrusuionService], (service: IngRollos_ExtrusuionService) => {
    expect(service).toBeTruthy();
  }));
});
