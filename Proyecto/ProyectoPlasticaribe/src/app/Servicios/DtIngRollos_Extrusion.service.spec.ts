/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DtIngRollos_ExtrusionService } from './DtIngRollos_Extrusion.service';

describe('Service: DtIngRollos_Extrusion', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DtIngRollos_ExtrusionService]
    });
  });

  it('should ...', inject([DtIngRollos_ExtrusionService], (service: DtIngRollos_ExtrusionService) => {
    expect(service).toBeTruthy();
  }));
});
