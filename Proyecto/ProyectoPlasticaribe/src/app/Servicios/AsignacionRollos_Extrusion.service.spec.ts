/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AsignacionRollos_ExtrusionService } from './AsignacionRollos_Extrusion.service';

describe('Service: AsignacionRollos_Extrusion', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AsignacionRollos_ExtrusionService]
    });
  });

  it('should ...', inject([AsignacionRollos_ExtrusionService], (service: AsignacionRollos_ExtrusionService) => {
    expect(service).toBeTruthy();
  }));
});
