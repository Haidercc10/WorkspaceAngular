/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Detalles_PrecargueDespachoService } from './Detalles_PrecargueDespacho.service';

describe('Service: Detalles_PrecargueDespacho', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Detalles_PrecargueDespachoService]
    });
  });

  it('should ...', inject([Detalles_PrecargueDespachoService], (service: Detalles_PrecargueDespachoService) => {
    expect(service).toBeTruthy();
  }));
});
