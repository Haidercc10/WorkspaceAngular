/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AsignacionBOPPService } from './asignacionBOPP.service';

describe('Service: AsignacionBOPP', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AsignacionBOPPService]
    });
  });

  it('should ...', inject([AsignacionBOPPService], (service: AsignacionBOPPService) => {
    expect(service).toBeTruthy();
  }));
});
