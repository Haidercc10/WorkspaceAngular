/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DetalleSolicitudMateriaPrimaService } from './DetalleSolicitudMateriaPrima.service';

describe('Service: DetalleSolicitudMateriaPrima', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DetalleSolicitudMateriaPrimaService]
    });
  });

  it('should ...', inject([DetalleSolicitudMateriaPrimaService], (service: DetalleSolicitudMateriaPrimaService) => {
    expect(service).toBeTruthy();
  }));
});
