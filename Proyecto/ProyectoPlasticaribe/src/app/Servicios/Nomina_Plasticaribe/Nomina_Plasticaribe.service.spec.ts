/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Nomina_PlasticaribeService } from './Nomina_Plasticaribe.service';

describe('Service: Nomina_Plasticaribe', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Nomina_PlasticaribeService]
    });
  });

  it('should ...', inject([Nomina_PlasticaribeService], (service: Nomina_PlasticaribeService) => {
    expect(service).toBeTruthy();
  }));
});
