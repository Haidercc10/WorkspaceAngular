/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DtPreEntregaRollosService } from './DtPreEntregaRollos.service';

describe('Service: DtPreEntregaRollos', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DtPreEntregaRollosService]
    });
  });

  it('should ...', inject([DtPreEntregaRollosService], (service: DtPreEntregaRollosService) => {
    expect(service).toBeTruthy();
  }));
});
