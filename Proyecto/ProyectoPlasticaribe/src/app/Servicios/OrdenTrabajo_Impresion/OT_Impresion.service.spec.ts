/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { OT_ImpresionService } from './OT_Impresion.service';

describe('Service: OT_Impresion', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [OT_ImpresionService]
    });
  });

  it('should ...', inject([OT_ImpresionService], (service: OT_ImpresionService) => {
    expect(service).toBeTruthy();
  }));
});
