/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Laminado_CapaService } from './Laminado_Capa.service';

describe('Service: Laminado_Capa', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Laminado_CapaService]
    });
  });

  it('should ...', inject([Laminado_CapaService], (service: Laminado_CapaService) => {
    expect(service).toBeTruthy();
  }));
});
