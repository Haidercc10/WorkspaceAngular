/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DetSolicitudMP_ExtrusionService } from './DetSolicitudMP_Extrusion.service';

describe('Service: DetSolicitudMP_Extrusion', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DetSolicitudMP_ExtrusionService]
    });
  });

  it('should ...', inject([DetSolicitudMP_ExtrusionService], (service: DetSolicitudMP_ExtrusionService) => {
    expect(service).toBeTruthy();
  }));
});
