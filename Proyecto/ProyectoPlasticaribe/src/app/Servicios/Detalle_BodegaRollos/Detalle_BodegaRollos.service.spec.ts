/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Detalle_BodegaRollosService } from './Detalle_BodegaRollos.service';

describe('Service: Detalle_BodegaRollos', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Detalle_BodegaRollosService]
    });
  });

  it('should ...', inject([Detalle_BodegaRollosService], (service: Detalle_BodegaRollosService) => {
    expect(service).toBeTruthy();
  }));
});
