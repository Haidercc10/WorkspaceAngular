/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { CajaMenor_PlasticaribeService } from './CajaMenor_Plasticaribe.service';

describe('Service: CajaMenor_Plasticaribe', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CajaMenor_PlasticaribeService]
    });
  });

  it('should ...', inject([CajaMenor_PlasticaribeService], (service: CajaMenor_PlasticaribeService) => {
    expect(service).toBeTruthy();
  }));
});
