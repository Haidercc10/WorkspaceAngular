/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { PreEntregaRollosService } from './PreEntregaRollos.service';

describe('Service: PreEntregaRollos', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PreEntregaRollosService]
    });
  });

  it('should ...', inject([PreEntregaRollosService], (service: PreEntregaRollosService) => {
    expect(service).toBeTruthy();
  }));
});
