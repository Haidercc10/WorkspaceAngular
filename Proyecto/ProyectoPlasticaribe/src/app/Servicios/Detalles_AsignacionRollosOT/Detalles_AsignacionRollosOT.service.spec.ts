/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Detalles_AsignacionRollosOTService } from './Detalles_AsignacionRollosOT.service';

describe('Service: Detalles_AsignacionRollosOT', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Detalles_AsignacionRollosOTService]
    });
  });

  it('should ...', inject([Detalles_AsignacionRollosOTService], (service: Detalles_AsignacionRollosOTService) => {
    expect(service).toBeTruthy();
  }));
});
