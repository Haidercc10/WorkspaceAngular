/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ControlCalidad_SelladoService } from './ControlCalidad_Sellado.service';

describe('Service: ControlCalidad_Sellado', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ControlCalidad_SelladoService]
    });
  });

  it('should ...', inject([ControlCalidad_SelladoService], (service: ControlCalidad_SelladoService) => {
    expect(service).toBeTruthy();
  }));
});
