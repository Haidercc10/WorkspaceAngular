/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ReImpresionEtiquetasService } from './ReImpresionEtiquetas.service';

describe('Service: ReImpresionEtiquetas', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ReImpresionEtiquetasService]
    });
  });

  it('should ...', inject([ReImpresionEtiquetasService], (service: ReImpresionEtiquetasService) => {
    expect(service).toBeTruthy();
  }));
});
