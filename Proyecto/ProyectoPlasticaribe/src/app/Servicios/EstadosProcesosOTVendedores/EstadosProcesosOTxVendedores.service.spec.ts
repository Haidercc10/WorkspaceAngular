/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { EstadosProcesosOTxVendedoresService } from './EstadosProcesosOTxVendedores.service';

describe('Service: EstadosProcesosOTxVendedores', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [EstadosProcesosOTxVendedoresService]
    });
  });

  it('should ...', inject([EstadosProcesosOTxVendedoresService], (service: EstadosProcesosOTxVendedoresService) => {
    expect(service).toBeTruthy();
  }));
});
