/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Asignacion_RollosOTService } from './Asignacion_RollosOT.service';

describe('Service: Asignacion_RollosOT', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Asignacion_RollosOTService]
    });
  });

  it('should ...', inject([Asignacion_RollosOTService], (service: Asignacion_RollosOTService) => {
    expect(service).toBeTruthy();
  }));
});
