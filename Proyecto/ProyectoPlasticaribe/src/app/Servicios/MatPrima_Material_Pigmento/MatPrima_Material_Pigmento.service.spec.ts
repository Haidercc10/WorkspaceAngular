/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { MatPrima_Material_PigmentoService } from './MatPrima_Material_Pigmento.service';

describe('Service: MatPrima_Material_Pigmento', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [MatPrima_Material_PigmentoService]
    });
  });

  it('should ...', inject([MatPrima_Material_PigmentoService], (service: MatPrima_Material_PigmentoService) => {
    expect(service).toBeTruthy();
  }));
});
