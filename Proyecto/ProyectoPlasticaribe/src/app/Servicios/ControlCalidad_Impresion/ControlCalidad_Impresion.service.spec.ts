/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ControlCalidad_ImpresionService } from './ControlCalidad_Impresion.service';

describe('Service: ControlCalidad_Impresion', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ControlCalidad_ImpresionService]
    });
  });

  it('should ...', inject([ControlCalidad_ImpresionService], (service: ControlCalidad_ImpresionService) => {
    expect(service).toBeTruthy();
  }));
});
