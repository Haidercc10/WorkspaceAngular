/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DtFacturacion_OrdenMaquilaService } from './DtFacturacion_OrdenMaquila.service';

describe('Service: DtFacturacion_OrdenMaquila', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DtFacturacion_OrdenMaquilaService]
    });
  });

  it('should ...', inject([DtFacturacion_OrdenMaquilaService], (service: DtFacturacion_OrdenMaquilaService) => {
    expect(service).toBeTruthy();
  }));
});
