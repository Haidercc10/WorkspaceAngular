/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DetallesAsgRollos_ExtrusionService } from './DetallesAsgRollos_Extrusion.service';

describe('Service: DetallesAsgRollos_Extrusion', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DetallesAsgRollos_ExtrusionService]
    });
  });

  it('should ...', inject([DetallesAsgRollos_ExtrusionService], (service: DetallesAsgRollos_ExtrusionService) => {
    expect(service).toBeTruthy();
  }));
});
