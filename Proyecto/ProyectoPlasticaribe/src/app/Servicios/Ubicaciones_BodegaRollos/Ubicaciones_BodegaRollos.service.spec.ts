/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Ubicaciones_BodegaRollosService } from './Ubicaciones_BodegaRollos.service';

describe('Service: Ubicaciones_BodegaRollos', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Ubicaciones_BodegaRollosService]
    });
  });

  it('should ...', inject([Ubicaciones_BodegaRollosService], (service: Ubicaciones_BodegaRollosService) => {
    expect(service).toBeTruthy();
  }));
});
