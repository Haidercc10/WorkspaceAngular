import { FiltrovendedorPipe } from './filtrovendedor.pipe';

describe('FiltrovendedorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrovendedorPipe();
    expect(pipe).toBeTruthy();
  });
});
