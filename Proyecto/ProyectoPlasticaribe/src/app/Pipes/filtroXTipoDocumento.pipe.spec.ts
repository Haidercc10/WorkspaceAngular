/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FiltroXTipoDocumentoPipe } from './filtroXTipoDocumento.pipe';

describe('Pipe: FiltroXTipoDocumentoe', () => {
  it('create an instance', () => {
    let pipe = new FiltroXTipoDocumentoPipe();
    expect(pipe).toBeTruthy();
  });
});
